#!/usr/bin/env node
/**
 * PANG — rebuild `PANG_VOICE_SYSTEM_PROMPT`.
 *
 * Placeholder. Iteration #3 wires the full string register and
 * generates the system prompt the narrative agent loads at call
 * time. Until then this script is a stub so the package.json entry
 * is stable and CI can reference it without a runtime error.
 */

console.log(
  "rebuild-voice-prompt: stub (voice register ships in iteration #3)",
);
