/**
 * PANG — verification surface voice corpus.
 *
 * The focused-work panel sits in the bottom-left quadrant of The Room
 * when a work is focused. Its landmark label is the only user-facing
 * literal the surface owns; artist / title / year are rendered from
 * the work's intake hint.
 *
 * Doctrine link: `PANG_Voice.md` § *Failure prose*.
 */

/** Landmark label for the focused-work panel `<aside>`. Sentence case. */
export const FOCUSED_WORK_LABEL = "focused work";
