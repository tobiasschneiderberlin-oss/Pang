/**
 * PANG — invite landing voice corpus.
 *
 * The invite-token landing surface (iter #9). The current iteration's
 * inline prose lives inside the component as a conditional ladder;
 * the landmark label is the single A25-flagged literal on this
 * surface. When the conditional prose migrates to corpus (a later
 * voice iteration), this module grows.
 *
 * Doctrine link: `PANG_Voice.md` § *Failure prose*.
 */

/** Landmark label on the invite `<main>`. Sentence case. */
export const INVITE_LANDING_LABEL = "invite";
