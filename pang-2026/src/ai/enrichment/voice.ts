/**
 * PANG — enrichment surface voice corpus.
 *
 * The enrichment panel sits to the right of the focused work once the
 * Enrichment Agent has populated provenance + artist context. The
 * panel's landmark label is the only user-facing literal the surface
 * owns directly; everything else inside is contributor-sourced or
 * AI-sourced text rendered through `data-pang-source`.
 *
 * Doctrine link: `PANG_Voice.md` § *Failure prose* — same register as
 * the chapter corpus.
 */

/**
 * Landmark label for the enrichment panel `<aside>`. Sentence case.
 * Names the contents, not the agent. Museumsschild.
 */
export const ENRICHMENT_PANEL_LABEL = "provenance and context";
