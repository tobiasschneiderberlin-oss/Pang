/**
 * A25 fixture — corpus stub.
 *
 * A module matching the A25 corpus convention: filename is `voice.ts`,
 * lives under `tests/fixtures/voice-v1/a25/corpus/`. Imports that
 * resolve to this file are accepted by A25.
 */

export const FIXTURE_CORPUS = Object.freeze({
  label: "sign in",
  title: "close",
  aria: {
    close: "close",
    next: "next",
  },
});
